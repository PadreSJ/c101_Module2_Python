#! usr/bin/env python

print "Let's find the sum two numbers!"

firstValue = raw_input("Type a number and press enter: ") # Gets user input for first number to add

secondValue = raw_input("Type another number and press enter: ") # Gets user input for second number to add

print "Your total is", int(firstValue) + int(secondValue) # Add the two values and prints them to the screen
